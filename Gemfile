# Gemfile
# ReCapture — Fastlane gem dependencies
# Run `bundle install` after creating this file.
# Run all Fastlane commands via `bundle exec fastlane` to use the locked versions.

source "https://rubygems.org"

ruby ">= 3.0.0"

gem "fastlane", "~> 2.220"
